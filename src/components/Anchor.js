import styled from "styled-components"
import { allCssProps, designTokens } from "~/styling"

const Anchor = styled.a`
	color: ${designTokens.colors.normal.blue};
	${allCssProps}
`
export default Anchor
